# Copy this file to `.env` (no extension before the dot — yes, a hidden file)
# and replace the value below with your real Google Maps API key.
# See README.md for how to get one (free $300 trial credit).

GOOGLE_MAPS_API_KEY=paste_your_google_maps_api_key_here
